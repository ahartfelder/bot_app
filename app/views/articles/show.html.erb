<p style="color: green"><%= notice %></p>

<%= render @article %>

<div>
  <%if logged_in? %>
  <%= link_to "Edit this article", edit_article_path(@article) %> | <% end %>
  <%= link_to "Back to articles", articles_path %>

  <%= button_to "Destroy this article", @article, method: :delete if logged_in? %>
</div>
