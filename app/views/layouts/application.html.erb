<!DOCTYPE html>
<html>
  <head>
    <title>BotApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if !logged_in? %>
      <script async src="https://telegram.org/js/telegram-widget.js?18" data-telegram-login="MyBlogFeedbackBot" data-size="large" data-auth-url="https://bot-ahart.herokuapp.com/login" data-request-access="write"></script>
    <% else %>
      <%= session[:username] %>
      <%= button_to 'Logout', logout_path, method: :delete %>
      <%= current_user.id if current_user %>
    <% end %>
    <hr>
    <%= yield %>
  </body>
</html>
